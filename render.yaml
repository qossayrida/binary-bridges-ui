services:
  - type: web
    name: binary-bridges-ui
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server.js  # Remove vite preview since your server.js handles it
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 10000  # Keep this consistent